# SuperNatural Motorsports - Environment Variables
# 
# Copy this file to .env and fill in your values.
# NEVER commit your .env file to version control!

# ============================================================================
# SUPABASE CONFIGURATION (Required)
# ============================================================================
# Get these from your Supabase project: https://supabase.com/dashboard/project/_/settings/api
# These are client-side safe (VITE_ prefix exposes them to the browser)

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================================================
# SERVER-SIDE ONLY (Optional - for scripts and admin)
# ============================================================================
# These should NEVER be exposed to the browser.
# Used for seeding scripts and admin operations only.

# SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================================================
# VERCEL BLOB STORAGE (Optional - for image uploads)
# ============================================================================
# Get from Vercel project settings: https://vercel.com/docs/storage/vercel-blob

# Public blob URL (client-side, for displaying images)
# This is the base URL where images are hosted
VITE_VERCEL_BLOB_URL=https://your-blob-store.public.blob.vercel-storage.com

# Blob token (server-side only, for upload scripts)
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...

# ============================================================================
# IMAGE GENERATION (Optional - for AI-generated car images)
# ============================================================================
# Google AI Studio API key for image generation scripts

# GOOGLE_AI_API_KEY=AIza...

# ============================================================================
# NOTES
# ============================================================================
# - VITE_* variables are exposed to the browser (client-side)
# - Non-VITE variables are server-side only (scripts, API routes)
# - For Vercel deployment, add these in the Vercel dashboard under Environment Variables
# - For local development, create a .env file in the project root
