/* OnboardingFlow - First-time User Onboarding Modal */
/* Cross-device compatible with multiple viewport fallbacks */

/* Overlay - full screen with solid background */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  width: 100%;
  height: 100%;
  /* Multiple height fallbacks for cross-device support */
  min-height: 100vh;
  min-height: 100dvh; /* Dynamic viewport - accounts for browser UI */
  min-height: -webkit-fill-available; /* iOS Safari specific */
  z-index: 1200;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--brand-border-subtle);
  /* Prevent any content from showing through */
  isolation: isolate;
}

/* Modal Container */
.modal {
  position: relative;
  width: 100%;
  max-width: 440px;
  max-height: 90vh;
  max-height: 90dvh;
  overflow: hidden;
  background: var(--brand-border-subtle);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: var(--radius-xl);
  display: flex;
  flex-direction: column;
}

/* ==========================================================
   UNIFIED PROGRESS BAR - Single continuous bar
   ========================================================== */
.progressBarContainer {
  padding: var(--space-lg) var(--space-xl) var(--space-md);
  flex-shrink: 0;
}

.progressBar {
  position: relative;
  width: 100%;
  height: 4px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-full);
  overflow: hidden;
}

.progressFill {
  position: absolute;
  top: 0;
  left: 0;
  height: 100%;
  background: linear-gradient(90deg, var(--sn-accent) 0%, var(--sn-primary) 100%);
  border-radius: var(--radius-full);
  transition: width 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.progressSteps {
  display: none; /* Hidden on mobile, shown on desktop */
}

/* REMOVE OLD PROGRESS DOTS - We only use the bar now */
.progressDots {
  display: none !important;
}

.dot, .dotActive, .dotCompleted {
  display: none !important;
}

/* Close Button - Removed (using "I'll do this later" link instead) */

/* ==========================================================
   STEP CONTENT - Vertically centered, no scroll on feature slides
   ========================================================== */
.stepContainer {
  flex: 1;
  overflow: hidden; /* Prevent scrolling - content must fit */
  padding: var(--space-sm) var(--space-lg);
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  min-height: 0; /* Allow flex shrinking */
}

/* Slide Animations */
.slideIn {
  animation: slideInContent 0.3s ease-out;
}

.slideOutLeft {
  animation: slideOutLeftContent 0.2s ease-in forwards;
}

.slideOutRight {
  animation: slideOutRightContent 0.2s ease-in forwards;
}

@keyframes slideInContent {
  from {
    opacity: 0;
    transform: translateX(20px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes slideOutLeftContent {
  from {
    opacity: 1;
    transform: translateX(0);
  }
  to {
    opacity: 0;
    transform: translateX(-20px);
  }
}

@keyframes slideOutRightContent {
  from {
    opacity: 1;
    transform: translateX(0);
  }
  to {
    opacity: 0;
    transform: translateX(20px);
  }
}

/* Step Title */
.stepTitle {
  font-family: var(--font-display);
  font-size: var(--font-size-2xl);
  font-weight: 600;
  color: var(--color-white);
  margin: 0 0 var(--space-sm);
  text-align: center;
  line-height: var(--line-height-tight);
}

/* Step Description */
.stepDescription {
  font-size: var(--font-size-sm);
  color: rgba(255, 255, 255, 0.6);
  line-height: 1.5;
  margin: 0 0 var(--space-lg);
  text-align: center;
  max-width: 340px;
}

/* ==========================================================
   INTENT CARDS - Multi-select with checkboxes
   ========================================================== */
.intentGrid {
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
  width: 100%;
  max-width: 400px;
}

.intentCard {
  display: flex;
  align-items: center;
  gap: var(--space-md);
  padding: var(--space-md) var(--space-lg);
  background: rgba(255, 255, 255, 0.03);
  border: 2px solid rgba(255, 255, 255, 0.08);
  border-radius: var(--radius-lg);
  cursor: pointer;
  transition: all 0.2s ease;
  position: relative;
  text-align: left;
}

.intentCard:hover {
  background: rgba(255, 255, 255, 0.06);
  border-color: rgba(255, 255, 255, 0.15);
}

.intentCard.selected {
  background: rgba(255, 77, 0, 0.1);
  border-color: var(--sn-accent);
}

.intentEmoji {
  font-size: 28px;
  line-height: 1;
  flex-shrink: 0;
}

.intentContent {
  flex: 1;
  min-width: 0;
}

.intentTitle {
  font-size: var(--font-size-base);
  font-weight: 600;
  color: var(--color-white);
  margin: 0 0 2px;
}

.intentDesc {
  font-size: var(--font-size-xs);
  color: rgba(255, 255, 255, 0.5);
  margin: 0;
}

/* Multi-select checkbox */
.intentCheckbox {
  width: 22px;
  height: 22px;
  border: 2px solid rgba(255, 255, 255, 0.25);
  border-radius: 6px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  transition: all 0.2s ease;
  background: transparent;
  color: transparent;
}

.intentCard.selected .intentCheckbox {
  background: var(--sn-accent);
  border-color: var(--sn-accent);
  color: var(--brand-border-subtle);
}

.intentHint {
  text-align: center;
  font-size: var(--font-size-xs);
  color: rgba(255, 255, 255, 0.4);
  margin-top: var(--space-md);
}

/* ==========================================================
   FEATURE TOUR - Now uses separate module: FeatureTourStep.module.css
   ========================================================== */
/* Legacy styles removed - Feature tour now has its own CSS module */

/* ==========================================================
   SELECTION CARDS (for referral)
   ========================================================== */
.optionGrid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--space-sm);
  width: 100%;
  max-width: 400px;
}

.optionCard {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-xs);
  padding: var(--space-md);
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: var(--radius-lg);
  cursor: pointer;
  transition: all 0.2s ease;
  text-align: center;
}

.optionCard:hover {
  background: rgba(255, 255, 255, 0.06);
  border-color: rgba(255, 255, 255, 0.15);
  transform: translateY(-2px);
}

.optionCard.selected {
  background: rgba(255, 77, 0, 0.1);
  border-color: var(--sn-accent);
}

.optionCardWide {
  grid-column: span 2;
}

.optionEmoji {
  font-size: 24px;
  line-height: 1;
}

.optionLabel {
  font-size: var(--font-size-sm);
  font-weight: 500;
  color: var(--color-white);
}

.optionSublabel {
  font-size: var(--font-size-xs);
  color: rgba(255, 255, 255, 0.4);
}

/* Other Input Field */
.otherInput {
  width: 100%;
  padding: var(--space-sm) var(--space-md);
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.15);
  border-radius: var(--radius-md);
  color: var(--color-white);
  font-size: var(--font-size-sm);
  margin-top: var(--space-sm);
  transition: all 0.2s ease;
}

.otherInput::placeholder {
  color: rgba(255, 255, 255, 0.3);
}

.otherInput:focus {
  outline: none;
  border-color: var(--sn-accent);
  background: rgba(255, 255, 255, 0.08);
}

/* ==========================================================
   EMAIL PREFERENCES (Final Step)
   ========================================================== */
.emailPrefs {
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
  width: 100%;
  max-width: 400px;
  margin-bottom: var(--space-lg);
}

.emailCheckbox {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  padding: var(--space-md);
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: var(--radius-md);
  cursor: pointer;
  transition: all 0.2s ease;
}

.emailCheckbox:hover {
  background: rgba(255, 255, 255, 0.05);
}

.emailCheckbox input {
  position: absolute;
  opacity: 0;
  width: 0;
  height: 0;
}

.checkmark {
  width: 20px;
  height: 20px;
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.2s ease;
  background: transparent;
  flex-shrink: 0;
}

.emailCheckbox input:checked + .checkmark {
  background: var(--sn-accent);
  border-color: var(--sn-accent);
  color: var(--brand-border-subtle);
}

.checkmarkIcon {
  opacity: 0;
  transition: opacity 0.2s ease;
}

.emailCheckbox input:checked + .checkmark .checkmarkIcon {
  opacity: 1;
}

.emailLabel {
  font-size: var(--font-size-sm);
  color: rgba(255, 255, 255, 0.8);
  flex: 1;
}

/* ==========================================================
   FOOTER NAVIGATION
   ========================================================== */
.footer {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-sm);
  padding: var(--space-md) var(--space-xl) var(--space-lg);
  border-top: 1px solid rgba(255, 255, 255, 0.06);
  background: var(--brand-border-subtle); /* Match modal background for consistency */
  flex-shrink: 0;
}

.navButtons {
  display: flex;
  gap: var(--space-sm);
  width: 100%;
  justify-content: center;
}

.backBtn {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 4px;
  padding: 12px 16px;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-md);
  font-size: var(--font-size-sm);
  font-weight: 600;
  color: rgba(255, 255, 255, 0.7);
  cursor: pointer;
  transition: all 0.2s ease;
  min-height: 44px;
  flex-shrink: 0;
}

.backBtn:hover {
  background: rgba(255, 255, 255, 0.08);
  color: var(--color-white);
}

/* Skip button - middle button between Back and Continue */
.skipBtn {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 12px 14px;
  background: transparent;
  border: 1px solid rgba(255, 255, 255, 0.15);
  border-radius: var(--radius-md);
  color: rgba(255, 255, 255, 0.6);
  font-size: var(--font-size-xs);
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
  min-height: 44px;
  white-space: nowrap;
}

.skipBtn:hover {
  background: rgba(255, 255, 255, 0.05);
  border-color: rgba(255, 255, 255, 0.25);
  color: rgba(255, 255, 255, 0.8);
}

.nextBtn {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  padding: 12px 24px;
  background: var(--sn-accent);
  border: none;
  border-radius: var(--radius-md);
  font-size: var(--font-size-sm);
  font-weight: 600;
  color: var(--brand-border-subtle);
  cursor: pointer;
  transition: all 0.2s ease;
  min-height: 44px;
}

.nextBtn:hover:not(:disabled) {
  filter: brightness(1.1);
  transform: translateY(-1px);
}

.nextBtn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

/* ==========================================================
   WELCOME STEP
   ========================================================== */
.welcomeIcon {
  width: 80px;
  height: 80px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, var(--sn-accent) 0%, var(--sn-primary) 100%);
  border-radius: var(--radius-xl);
  margin: 0 auto var(--space-xl);
  color: var(--color-white);
}

.welcomeTitle {
  font-family: var(--font-display);
  font-size: var(--font-size-3xl);
  font-weight: 600;
  color: var(--color-white);
  margin: 0 0 var(--space-sm);
  text-align: center;
  line-height: var(--line-height-tight);
}

.welcomeTagline {
  font-size: var(--font-size-base);
  color: rgba(255, 255, 255, 0.6);
  line-height: 1.5;
  text-align: center;
  max-width: 320px;
  margin: 0 auto;
}

.welcomeHighlight {
  color: var(--sn-accent);
  font-weight: 600;
}

/* ==========================================================
   FINAL STEP - Clean, focused completion screen
   ========================================================== */
.finalStep {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  text-align: center;
}

.finalContent {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-lg);
}

.ctaCard {
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-lg);
  padding: var(--space-xl);
  text-align: center;
  max-width: 320px;
}

.ctaIcon {
  font-size: 56px;
  margin-bottom: var(--space-sm);
}

.ctaTitle {
  font-family: var(--font-display);
  font-size: var(--font-size-lg);
  font-weight: 600;
  color: var(--color-white);
  margin: 0 0 var(--space-xs);
}

.ctaDesc {
  font-size: var(--font-size-sm);
  color: rgba(255, 255, 255, 0.5);
  margin: 0;
}

/* ==========================================================
   MOBILE: FULL SCREEN - Cross-device compatible
   Tested for: iOS Safari, Chrome, Android Chrome, Samsung Browser
   ========================================================== */
@media (max-width: 480px) {
  .overlay {
    /* Reset to ensure full coverage */
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    /* Fallback chain for different browsers */
    min-height: 100vh;
    min-height: 100dvh;
    min-height: -webkit-fill-available;
    /* No flex alignment - modal will be absolute positioned */
    display: block;
  }
  
  .modal {
    /* Absolute positioning within fixed overlay */
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    /* Fallback chain */
    min-height: 100vh;
    min-height: 100dvh;
    min-height: -webkit-fill-available;
    max-width: none;
    max-height: none;
    border-radius: 0;
    border: none;
    /* Flex for content layout */
    display: flex;
    flex-direction: column;
  }
  
  .progressBarContainer {
    padding: var(--space-md) var(--space-lg) var(--space-sm);
    /* iOS safe area for notch/dynamic island */
    padding-top: calc(var(--space-md) + env(safe-area-inset-top, 0px));
    flex-shrink: 0;
  }

  .stepContainer {
    flex: 1;
    padding: var(--space-xs) var(--space-md);
    min-height: 0;
    overflow: hidden;
  }
  
  .footer {
    padding: var(--space-sm) var(--space-md);
    /* iOS safe area for home indicator */
    padding-bottom: calc(var(--space-sm) + env(safe-area-inset-bottom, 0px));
    gap: var(--space-xs);
    flex-shrink: 0;
  }
  
  .optionGrid {
    grid-template-columns: 1fr;
  }
  
  .optionCardWide {
    grid-column: span 1;
  }
  
  .welcomeIcon {
    width: 72px;
    height: 72px;
    margin-bottom: var(--space-lg);
  }
  
  .welcomeTitle {
    font-size: var(--font-size-2xl);
  }
  
  .welcomeTagline {
    font-size: var(--font-size-sm);
  }
}

/* ==========================================================
   TABLET AND UP
   ========================================================== */
@media (min-width: 768px) {
  .modal {
    max-width: 480px;
    min-height: 500px;
  }
  
  .stepContainer {
    padding: var(--space-xl) var(--space-2xl);
  }
  
  .progressSteps {
    display: flex;
    justify-content: space-between;
    margin-top: 8px;
    padding: 0 2px;
  }
  
  .progressStep {
    font-size: 10px;
    color: rgba(255, 255, 255, 0.3);
    transition: color 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    font-weight: 500;
  }
  
  .progressStep.active {
    color: var(--sn-accent);
  }
  
  .progressStep.completed {
    color: rgba(255, 255, 255, 0.5);
  }
  
  .welcomeIcon {
    width: 96px;
    height: 96px;
  }
  
  .welcomeTitle {
    font-size: var(--font-size-4xl);
  }
}
