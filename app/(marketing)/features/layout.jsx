/**
 * Features Layout
 * 
 * NOTE: Features section is deprecated but files remain for potential future use.
 * This is a simple pass-through layout.
 */

export default function FeaturesLayout({ children }) {
  return children;
}
